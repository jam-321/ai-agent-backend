<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jam.agent.agent.persistence.mapper.ConversationTurnAttachmentMapper">
    <resultMap id="AttachmentResult" type="com.jam.agent.agent.persistence.entity.ConversationTurnAttachmentEntity">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="turnId" column="turn_id"/>
        <result property="assetId" column="asset_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="detail" column="detail"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    <select id="selectForTurn" resultMap="AttachmentResult">
        SELECT a.* FROM conversation_turn_attachment a
        JOIN conversation c ON c.id = a.conversation_id
        WHERE c.user_id = #{userId} AND c.is_deleted = 0
          AND a.conversation_id = #{conversationId} AND a.turn_id = #{turnId}
        ORDER BY a.sort_order, a.id
    </select>
    <select id="selectHistory" resultMap="AttachmentResult">
        SELECT a.* FROM conversation_turn_attachment a
        JOIN conversation c ON c.id = a.conversation_id
        WHERE c.user_id = #{userId} AND c.is_deleted = 0
          AND a.conversation_id = #{conversationId} AND a.turn_id &lt; #{currentTurnId}
        ORDER BY a.turn_id, a.sort_order, a.id
    </select>
</mapper>
