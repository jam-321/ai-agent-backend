# 2026-08-15：工程拆分与 Docker 环境

## 本次推进

- 确认主线工程从单一 `ai-agent` 仓库拆分为两个独立仓库：
  - 后端：`E:\ai\code\code\ai-agent-backend`，对应 `jam-321/ai-agent-backend`
  - 前端：`E:\ai\code\code\ai-agent-frontend`，对应 `jam-321/ai-agent-frontend`
- 后端 Maven 坐标调整为 `com.jam:ai-agent-backend`。
- 后端使用 JDK 17 + Spring Boot 3.5 + Spring AI。
- 前端使用 Vue 3 + Vite，开发端口统一为 `8081`，`/api` 代理到后端 `8080`。
- 安装并启用 Docker Desktop、WSL 和 `VirtualMachinePlatform`。
- Docker 中启动 MySQL 8.4 与 Redis 7，端口仅绑定 `127.0.0.1`。
- 验证 MySQL 健康检查、数据库 `ai_agent` 创建、Redis `PING` 返回 `PONG`。

## 记录规则

- 迭代历史按日期或显著里程碑单独建文件。
- 当单文件过长或进入新阶段时，新开 Markdown 文件。
- 每条记录优先保存决策、路径、验证结果和遗留问题，不复制长日志。
