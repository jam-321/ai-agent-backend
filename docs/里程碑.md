# 主线里程碑

> 更新时间：2026-08-21。本文件是主线进度的第一入口，保持短摘要；详细过程看 `docs/迭代历史/`。

## 当前状态

AgentLoop MVP v1 已完成：系统具备会话持久化、跨轮短期记忆、手动 Tool Calling、工具异常回注、过程事件轮询和会话级并发控制能力。当前补齐了 AgentLoop 代码可读性、轻量管理员身份、运行监控页，并完成后端功能模块化包结构改造。

## 已完成

- 前后端从一体仓库拆分为独立工程：
  - 后端：`E:\ai\code\code\ai-agent-backend`，GitHub `jam-321/ai-agent-backend`
  - 前端：`E:\ai\code\code\ai-agent-frontend`，GitHub `jam-321/ai-agent-frontend`
- 后端完成 Spring Boot 3.5 + Spring AI + Java 17 初始化，Maven 打包成功。
- 前端完成 Vue 3 + Vite 初始化，生产构建成功，开发端口为 `8081`。
- 基础接口与页面链路已验证：前端 `/api` 代理到后端 `8080`，未配置 `DEEPSEEK_API_KEY` 时返回 mock 回复。
- 已安装 Docker Desktop、WSL、Docker Engine 与 Docker Compose。
- 已在 Docker 中启动并验证 MySQL 8.4 和 Redis 7，端口仅绑定 127.0.0.1。
- 已配置 DEEPSEEK_API_KEY（用户级环境变量），并验证真实 DeepSeek 对话：/api/chat 返回真实回复（2026-08-15）。
- 已实现用户注册、登录、退出登录和当前用户接口：`/api/auth/register`、`/api/auth/login`、`/api/auth/logout`、`/api/auth/me`。
- 已新增 `app_user` 用户表，用户名唯一，密码使用 BCrypt 哈希保存，不保存明文密码。
- 已接入 Spring Session Data Redis，Session 默认保存 7 天，浏览器通过 `HttpOnly` Cookie 携带 Session ID。
- 已通过 Spring Security 默认保护业务接口，未登录请求不能访问 `/api/chat`；认证接口和 `/api/ping` 保持放行。
- 前端已增加登录/注册界面、刷新页面恢复登录状态、右上角用户名显示和退出按钮；注册成功后不自动登录。
- 已验证 MySQL 8.4 与 Redis 7 容器健康，前端生产构建成功，后端在 JDK 17 下编译打包成功。
- 已验证注册接口返回 `201`，登录接口返回当前用户名；Redis Session 序列化问题已修复为可序列化认证主体。
- 新增 `conversation`、`conversation_turn`、`conversation_node` 三张 canonical 表；完成会话列表、历史查询、软删除和用户隔离。
- `POST /api/chat` 改为异步 `202` 提交，`GET /api/chat/progress` 返回 `REASONING / COMPLETE / ERROR` 和聚合后的执行步骤。
- 完成最多 2 次 Attempt、最多 40 个 Model Round 的手动 AgentLoop，关闭 Spring AI 自动工具执行。
- 首批工具已上线：`current_time`、`calculate`、`query_conversation_node`；支持同轮多工具并行执行和按模型原顺序回填。
- MySQL 重建最近 50 个完整 Turn，包含工具调用对，并增加字符预算和合法 JSON 截断，实现服务重启后继续对话。
- Redis Conversation Lock 保证同一会话只运行一个 Turn；线程池拒绝、运行异常和重启残留统一补成 ERROR 终态。
- 前端完成会话侧栏、切换/新建/删除、历史消息、1.5 秒进度轮询、工具步骤卡片和刷新恢复未完成 Turn。
- 真实 DeepSeek 验收通过：时间工具、精确乘法、第二轮基于历史结果继续计算、用户隔离 404、同会话并发 409。
- AgentLoop 核心代码已补充边界注释、分层方法和 SQL 文本块，Node 并发写入改为使用当前 INSERT 的生成主键。
- 后端持久化层已从 `JdbcTemplate` 统一迁移到 MyBatis-Plus 3.5.12：Entity + BaseMapper + Mapper XML，业务代码中不再保留 JDBC 直接调用。
- `app_user` 新增 `is_admin`，登录响应、`/api/auth/me` 和 Redis Session 均携带管理员标记；旧数据库由幂等启动迁移补列。
- 新增管理员监控接口：`/api/admin/monitor/overview`、`/conversations`、`/conversations/{id}`、`/tools`；接口由 `ROLE_ADMIN` 保护，不返回密码信息。
- 前端新增管理员“监控”视图，展示运行总览、会话分页、工具统计和 Turn/Node 详情；管理员仍可正常使用会话页。
- 验证通过：普通用户管理接口 `403`，管理员总览/会话分页/详情/工具统计 `200`，管理员普通会话接口 `200`；后端 Maven 测试打包和前端生产构建通过。
- 后端包结构由全局 `controller/service/mapper/repository` 横向分层调整为功能模块优先：`agent`、`auth`、`conversation`、`monitoring`、`common`；模块内部保留熟悉的 Controller、Service、DTO、Persistence 分层。
- Entity、Mapper、Repository 和 Mapper XML 已跟随数据归属迁入对应模块；删除无调用方的早期 `ChatService`，当前 Agent 调用链统一从 `AgentRunService` 进入 AgentLoop。
- 已将试验场验证的事件插件架构迁入主工程：AgentLoop 固定发射事件，Dispatcher/Registry 负责分发，NodeTracePlugin 负责轨迹投影，TimeInjectPlugin 作为可选插件。
- 新增 `agent_config` Agent 配方和 `conversation.agent_key` 会话绑定；每轮捕获系统提示词与启用插件集合，支持在下一轮切换 Agent 配方。
- 新增 Agent 执行类型路由：`LOOP` 使用 AgentLoop，`WORKFLOW` 使用独立的轻量工作流执行器；新增 `com.jam.agent.workflow` 包，内置 `time_workflow -> time_report` 示例并完成真实 HTTP 验证。
- Turn 准备阶段已统一提升到所有执行器和 Attempt 之前，`turn_start` 每轮只发一次，重试使用互不污染的消息副本。
- 新增 `model_provider_config` 供应商表和 `ModelRegistry`，Agent 配方可独立选择供应商、模型名和 temperature，会话下一轮切换 Agent 时同步切换模型；公开 Agent 接口不返回 API Key。
- Tool 定义迁入 `agent.tool.definition` 并按时间、计算、会话领域拆分；`ToolRegistry` 自动发现 Provider，`ToolExecutor` 不再直接注册具体工具类。
- 保留主工程的异步执行、OuterLoop 重试、并行工具调用、Redis 会话锁、重启恢复和短期记忆；工具事件使用独立快照，避免并行工具共享可变工具字段。

## 下一步

- 由用户进行完整页面体验回归，收集 AgentLoop 初版交互和回答质量问题。
- 增加 AgentLoop、Progress 聚合、工具失败回注和多工具乱序完成的系统化自动测试。
- 根据真实长会话数据评估 tokenizer、上下文压缩和 Node 大字段拆表时机。
- Agent 能力定位确定后，补充面向实际业务的 Tool、Skill 或 RAG 能力。
- 使用 DBeaver 查看 `conversation`、`conversation_turn`、`conversation_node`，将数据库记录与监控页和 AgentLoop 日志逐项对照。
- 由用户继续研究工作流执行器、步骤定义和 `AgentExecutorRegistry`，再根据实际业务决定是否增加数据库定义、可视化编排和工作流断点恢复。
