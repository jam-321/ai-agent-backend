<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jam.agent.agent.persistence.mapper.ConversationMemorySummaryMapper">
    <select id="selectLatestOwned" resultType="com.jam.agent.agent.persistence.entity.ConversationMemorySummaryEntity">
        SELECT s.*
        FROM conversation_memory_summary s
        JOIN conversation c ON c.id = s.conversation_id
        WHERE s.conversation_id = #{conversationId}
          AND c.user_id = #{userId}
          AND c.is_deleted = 0
        ORDER BY s.covered_until_turn_id DESC, s.id DESC
        LIMIT 1
    </select>
</mapper>
